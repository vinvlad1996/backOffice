import { getTestSelectors } from '~/shared/utils/unit-test/test-id';

export const COMPONENT_NAME = 'partners-sponsor-info';

export enum PartnersSponsorInfoTextAttribute {
  //
}

export enum PartnersSponsorInfoTestLocator {
  //
}

export const dtPartnersSponsorInfo = getTestSelectors(COMPONENT_NAME, PartnersSponsorInfoTestLocator);
